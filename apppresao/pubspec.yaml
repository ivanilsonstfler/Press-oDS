name: apppresao
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '^3.10.0'   # <= ESSA LINHA É O QUE ELE ESTÁ PEDINDO

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  provider: ^6.1.2
  crypto: ^3.0.3

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  fl_chart: ^0.69.0
  share_plus: ^10.0.0
  flutter_local_notifications: ^17.2.1
  local_auth: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
